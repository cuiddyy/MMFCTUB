# Table Images & Test Samples Corresponding Relationship
1. This folder stores table images for the Credit Table Understanding (CTU) task. The filename of each table image in this folder corresponds to the content of the `latex_single_file_path` field in each instance of the `test_samples.json` file.
2. These table images are in `.png` format, which is converted from PDF files compiled and generated by LaTeX table code.
